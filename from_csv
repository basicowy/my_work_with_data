# data format
#show_id,type,title,director,cast,country,date_added,release_year,duration,description,genre
#s2,Movie,7:19,Jorge Michel Grau,"Demián Bichir, Héctor Bonilla, Oscar Serrano, Azalia Ortiz, Octavio Michel, Carmen Beato",Mexico,"December 23, 2016",2016,93,"After a devastating earthquake hits Mexico City, trapped survivors from all walks of life wait to be rescued while trying desperately to stay alive.",Dramas

import pandas as pd
netflix_df = pd.read_csv("netflix_data.csv")
only_1990 = netflix_df[(netflix_df['release_year'] >= 1990) & (netflix_df['release_year'] < 2000)]
duration = only_1990['duration'].mode().iloc[0]
short_movie_count = only_1990[
    (only_1990['duration'] < 90) &
    (only_1990['type'] == 'Movie') &
    (only_1990['genre'] == 'Action')
].shape[0]
print(f" the most frequent movie duration in the 1990s :{duration} minutes")
print(f"short action movies(duration under 90 minutes) released in the 1990s : {short_movie_count}")
